[project]
name = "pcos-detection"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "scikit-learn",
    "pandas",
    "fastapi[standard]",
    "uvicorn>=0.34.0",
]
